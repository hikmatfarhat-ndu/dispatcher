name: Dispatcher
on:
  repository_dispatch:
jobs:
  dispatch:
    runs-on: [ubuntu-latest]
    steps:
      #- uses: actions/checkout@v2
      #  with:
      #      ref: ${{github.head_ref}}
      - name: set secret
        run: |
          echo "reveived hook"
          suffix="${{github.event.client_payload.repo}}"
          res=$(echo "$suffix"|cut -d '/' -f 2|grep 'homework')
          if [ "$res" -ne "" ]
          then
            echo "homework"
          fi
          echo "$res"

      #- name: PrintEnv
      #  env:
      #    GITHUB_CONTEXT: ${{ toJSON(github) }}
      #  run: |
      #    echo event triggered.
      #    echo "$GITHUB_CONTEXT"
